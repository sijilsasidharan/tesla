import { ActiveVariantPipe } from './active-variant.pipe';

describe('ActiveVariantPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveVariantPipe();
    expect(pipe).toBeTruthy();
  });
});
