import { GroupByMotorPipe } from './group-by-motor.pipe';

describe('GroupByMotorPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByMotorPipe();
    expect(pipe).toBeTruthy();
  });
});
